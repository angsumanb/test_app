<% provide(:title, "Edit suite") %> 
<h1>Update suite details</h1>

<div class="row-fluid">
  <div class="span8">
    <%= form_for(@suite) do |f| %>
      <%= render 'shared/error_messages', object: f.object %>

      <%= f.label :name %>
      <%= f.text_field :name %>

      <%= f.label :description %>
      <%= f.text_area :description, size: "40*20" %>
</br>
      <%= f.submit "Save changes", class: "btn btn-primary" %>
    <% end %>

  </div>
</div>
